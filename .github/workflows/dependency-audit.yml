name: dependency-audit

# Fails on any known dependency vulnerability or unavailable audit report.

on:
  schedule:
    - cron: "23 6 * * 1" # Mondays 06:23 UTC, after Dependabot opens its PRs
  pull_request:
    paths:
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/dependency-audit.yml"
      - "scripts/audit-ci.mjs"
      - "scripts/test-audit-ci.mjs"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "22"
          cache: npm

      - name: Install (lockfile-exact)
        run: npm ci --no-audit --no-fund

      - name: Test audit gate
        run: node --test scripts/test-audit-ci.mjs

      - name: Audit gate
        run: node scripts/audit-ci.mjs
