// Representative routes for the landing-page smoke suites. Keep this list small —
// these are smoke checks, not full coverage.
export const A11Y_ROUTES = ['/', '/about/', '/404.html'];

// High-risk pages for the responsive sweep (single-page hub — the homepage is
// the whole product).
export const RESPONSIVE_ROUTES = ['/'];
